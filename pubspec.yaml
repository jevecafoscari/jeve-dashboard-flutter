name: jeve_dashboard_flutter
description: A new Flutter project.


publish_to: 'none' # Remove this line if you wish to publish to pub.dev


version: 1.0.0+1

environment:
  sdk: '>=2.18.4 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Firebase
  firebase_core: ^2.1.1

  # Grafica
  cupertino_icons: ^1.0.5
  swatcher: ^0.1.0
  r_flutter: ^0.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.1
  build_runner: ^2.3.2


flutter:
  uses-material-design: true

  assets:
    - assets/

